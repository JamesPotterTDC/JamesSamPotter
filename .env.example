# This is a root-level .env example for reference
# Copy backend/.env.example and frontend/.env.example for actual configuration

# Backend configuration is in backend/.env
# Frontend configuration is in frontend/.env
